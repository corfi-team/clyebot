const { MessageEmbed } = require("discord.js")
module.exports = {
    name: "serwery",
    aliases: ["s"],
    run: async ({ msg, args, Discord, client }) => {
 console.log("Wywołano logowanie serwerów")
    }
  }